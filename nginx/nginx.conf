upstream web {
    server web:5000;
}

server {
    listen 80;
    server_name your_domain.com;
    return 301 https://$host$request_uri;
}
## AUTO-GENERATED FILE (may be overwritten by scripts/setup_saltvault.py)

# HTTP server (will redirect to HTTPS if SSL enabled by template rendering)
server {
    listen 80;
    server_name placeholder.local;
    return 301 https://$host$request_uri;
}

# HTTPS server block placeholder; replaced by template if certificates exist
server {
    listen 443 ssl;
    server_name placeholder.local;
    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    location / {
        proxy_pass http://web;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


server {
    listen 443 ssl;
    server_name domain_name;

    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    location / {
        proxy_pass http://web;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
